const ProfileContentAndTabs = ({ user }) => {
  return (
    <div className="profile_content_and_tabs">
      <h1>Profile content and tabs of {user.name}</h1>
    </div>
  );
};

export default ProfileContentAndTabs;
